<b:style src="layout.css" />

<div class="view view_{expand}">
    <div class="menu">
        <div class="context">place for context</div>
        <div class="board-list__wrap" style="-webkit-transform: translate(0px, -51px) scale(1) translateZ(0px);">
            <div id="pullDown" class="">
                <span class="pullDownIcon"></span><span class="pullDownLabel">Pull down to refresh...</span>
            </div>
            <!--{boardList}-->
        </div>
        <!--{loggedUser}-->
    </div>
    <div class="page">
        <div class="bar-header"><!--{toggleMenu}--></div>
        <!--{boardGrid}-->
    </div>
</div>
